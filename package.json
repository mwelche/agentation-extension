{
  "name": "agentation-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && echo 'Load dist/ in chrome://extensions (Developer mode)'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.25.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "@types/chrome": "^0.0.287",
    "sass": "^1.97.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0"
  }
}
